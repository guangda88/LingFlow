# LingFlow Requirements

# Core dependencies
tiktoken>=0.5.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional dependencies (for full functionality)
# sqlite3 (included in Python standard library)
