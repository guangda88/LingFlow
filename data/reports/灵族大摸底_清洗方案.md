# 灵族大家庭大摸底 — 清洗方案

> 扫描日期: 2026-04-17
> 扫描范围: /home/ai/ 下全部灵族项目（21个）
> 原则: **只分析不动手，所有操作需人工确认后执行**

---

## 一、总体概况

| 指标 | 数值 |
|------|------|
| 项目总数 | 21 |
| 有 Git 的 | 14 |
| 无 Git 的 | 5（LingLaw, LingWeb, ai-server, ling-protocol, ling-family-docs）|
| 总磁盘占用 | ~40 GB |
| 估算可回收 | 19–65 GB |
| 活跃项目（7天内有提交）| 8 |
| 休眠项目（>2周无提交）| 4 |
| 跨项目 sys.path.insert | 25+ 处 |
| 重复代码量 | ~4,000 行 |

---

## 二、优先级 P0 — 安全风险（立即处理）

### P0-1. DashScope API Key 泄露（10个文件）

**密钥**: `[REDACTED_DASHSCOPE_KEY]` 硬编码在以下文件中：

```
lingtongask/generate_ep37.py:11
lingtongask/clone_v3_flash.py:8
lingtongask/generate_ep034_longshuo.py:6
lingtongask/generate_ep034_compare.py:6
lingtongask/generate_ppt.py:25
lingtongask/generate_video_ep36style.py:17
lingtongask/generate_video_subtitle.py:22
lingtongask/episodes/ep032/synth_v3.py:21
lingtongask/scripts/test_tts_quality.py:79
lingtong_daemon.py:19
```

**操作**:
1. 去 [DashScope 控制台](https://dashscope.console.aliyun.com/) 轮换此 Key
2. 将 Key 移入 `.env` 文件，代码改为 `os.environ.get("DASHSCOPE_API_KEY")`
3. 确认 `lingtongask/.gitignore` 已包含 `.env`

### P0-2. GitHub PAT 泄露（训练数据集）

**密钥**: `[REDACTED_PAT]` 和 `[REDACTED_PAT]`

出现在 `lingresearch/data/training_dataset/` 下的 5 个 JSONL 文件中。

**操作**:
1. 去 GitHub Settings → Developer Settings → Personal Access Tokens 删除这两个 Token
2. 这些是训练数据集里的样本，清理方式选择：
   - 方案A: `sed` 替换为 `[REDACTED]`
   - 方案B: 不改动数据，但确认 Token 已失效

### P0-3. 阿里云 AK/SK 泄露

**AccessKey**: `[REDACTED_ALIYUN_AK]`

出现在:
- `lingresearch/data/training_dataset/` 多个 JSONL 文件
- `.claude/history.jsonl` (Claude 会话历史)
- `.claude/projects/` 下的 JSONL 文件

**操作**:
1. 去 [阿里云 RAM 控制台](https://ram.console.aliyun.com/) 禁用此 AK
2. Claude 历史文件无法修改，但确保 Token 已失效即可

### P0-4. GLM API Key

**密钥**: `[REDACTED_GLM_KEY]`

在 `/home/ai/.lingflow/external_servers.json` 中。

**操作**: 确认此文件不在任何 Git 仓库中。

---

## 三、优先级 P1 — 磁盘回收（大文件清理）

### P1-1. ML 训练 Checkpoint — 5–15 GB

**路径**: `/home/ai/lingresearch/models/`

| 内容 | 建议 |
|------|------|
| `embedding_model/` (旧版，已被 v2 取代) | **删除整个目录** |
| `embedding_model_v2/checkpoints/` (9个checkpoint) | **保留最终版，删除其余** |

```bash
# 操作命令（确认后执行）
rm -rf /home/ai/lingresearch/models/embedding_model/
rm -rf /home/ai/lingresearch/models/embedding_model_v2/checkpoints/
```

### P1-2. Bilibili 归档视频 — 5–20 GB

**路径**: `/home/ai/lingtongask/archive/bilibili/`

40集 × (mp4 + flac)，每集约 100–350 MB。

```bash
# 如果已上传 B站，本地可删除
find /home/ai/lingtongask/archive/bilibili/ep* -name "*.mp4" -delete
find /home/ai/lingtongask/archive/bilibili/ep* -name "*.flac" -delete
```

### P1-3. Shorts 归档 — 0.5–2 GB

**路径**: `/home/ai/lingtongask/archive/shorts/`

```bash
rm -rf /home/ai/lingtongask/archive/shorts/ep*/*.mp4
```

### P1-4. Compressed 已发布视频 — 2–5 GB

**路径**: `/home/ai/lingtongask/compressed/`

EP037–EP051 的 `_edge.mp3` + `_final.mp4`。已发布可删除。

```bash
rm -rf /home/ai/lingtongask/compressed/
```

### P1-5. Episodes 临时文件 — 2–10 GB

**路径**: `/home/ai/lingtongask/episodes/`

```bash
# 删除临时剪辑帧
find /home/ai/lingtongask/episodes/ -type d -name "tmp_clips" -exec rm -rf {} +
find /home/ai/lingtongask/episodes/ -type d -name "frames" -exec rm -rf {} +
# 删除测试音频
rm -rf /home/ai/lingtongask/episodes/female_pitch_test/
rm -rf /home/ai/lingtongask/episodes/female_rate_test/
rm -rf /home/ai/lingtongask/episodes/audio_comparison/
```

### P1-6. .wine 目录 — 0.5–2 GB

**路径**: `/home/ai/.wine/`

```bash
# 如果不需要 Wine
rm -rf /home/ai/.wine/
```

### P1-7. zhineng-knowledge-system 重复 venv — 0.5–2 GB

3 个独立 venv:
- `services/web_app/backend/venv/`
- `services/ai_service/venv/`
- `mvp/venv/`

**操作**: 合并为一个，或删除后按需重建。

### P1-8. zhineng_knowledge-system（下划线版）— 废弃目录

**路径**: `/home/ai/zhineng_knowledge-system/`（注意下划线，与 hyphen 版不同）

仅含 3 个文件，疑似误操作创建。

```bash
rm -rf /home/ai/zhineng_knowledge-system/
```

### P1-9. .benchmarks 目录 — ~50 MB

16 个项目各有 `.benchmarks/`。

```bash
find /home/ai -type d -name ".benchmarks" -exec rm -rf {} +
```

### P1-10. 所有 .bak/.backup/.old 文件 — ~100 MB

所有原始文件均已确认存在，备份文件可安全删除。

```bash
find /home/ai -name "*.bak" -not -path "*/.venv/*" -not -path "*/venv/*" -not -path "*/.git/*" -delete
find /home/ai -name "*.backup.*" -not -path "*/.venv/*" -not -path "*/.git/*" -delete
```

### P1-11. zhineng-knowledge-system 备份

```bash
rm -rf /home/ai/zhineng-knowledge-system/backups/
rm -rf /home/ai/zhineng-knowledge-system/data/processed/textbooks_v2_backup_*/
rm -rf /home/ai/zhineng-knowledge-system/data/textbooks_duplicate_backup/
```

---

## 四、优先级 P2 — 代码质量

### P2-1. LingFlow 死代码（~1,560 行，已验证）

> 注意：初版扫描方法有误判（只搜 basename 未考虑包路径导入）。
> 以下为二次验证后确认的真实死代码：

| 模块 | 行数 | 状态 |
|------|------|------|
| `lingflow/communication/` (整个包) | ~950 | 0 个外部引用，只有自带测试 |
| `lingflow/infrastructure/cluster.py` | 267 | 0 外部引用 |
| `lingflow/ai_friendly.py` | 347 | 0 外部引用 |

**已被排除的误判**（通过 `__init__.py` 导出，实际在用）：
- coordinator.py — `lingflow/__init__.py` 核心 lazy import
- models.py, exceptions.py, sandbox.py — 被 coordinator 等间接引用
- monitoring/ — 被 layered_skill_loader + 测试引用
- constitution.py, compliance_matrix.py, layered_skill_loader.py, prompt_router.py — 通过 `core/__init__.py` 导出

**建议**: 将 communication/, infrastructure/, ai_friendly.py 标记为 deprecated 或删除。删除前跑全量测试确认。

### P2-2. LingFlow 过度工程

| 模块 | 声称 | 实际 | 问题 |
|------|------|------|------|
| **33 个 Skills** | 全有 SKILL.md + implementation.py | **0 个被主代码导入** | 全部是文档/桩代码 |
| **6 个 Agent 类型** | agents.json + agents.v2.json | 全是通用 Agent(config)，无特化行为 | V2 格式从未被读取 |
| **self_optimizer Phase 4-5** | 贝叶斯优化 + 学习系统 | 有真实实现但无人调用 | 过度工程 |
| **monitoring** | operations_monitor | **0 个主代码引用** | 死代码 |

### P2-3. lingtongask 根目录脚本（25个一次性脚本）

```
generate_ep034_audio.py    generate_ep034_compare.py
generate_ep034_longshuo.py generate_ep37.py
generate_ep37_edge.py      generate_ep37_video.py
generate_edge.py           generate_edge_batch.py
generate_hq_audio.py       generate_ppt.py
generate_video_ep36style.py generate_video_subtitle.py
one_click_video.py         optimize_ep034.py
optimize_ep034_r2.py       optimize_audio_post.py
pptx2video.py              pptx_renderer.py
batch_pptx2video.py        polyphone_fix.py
regenerate_ep37_video.py   fix_ep37_hang.py
smart_slide_video.py       extract_ppt_assets.py
simulate_publish.py
```

**建议**: 归档到 `lingtongask/archive/scripts/` 或删除。保留有持续价值的脚本（如 `smart_slide_video.py`, `one_click_video.py`）移入 `scripts/`。

### P2-4. 跨项目代码重复（~4,000 行）

| 重复项 | 涉及项目 | 行数 | 建议 |
|--------|----------|------|------|
| self_optimizer 子系统 | LingFlow ↔ LingClaude | ~1,500 | 提取为 `ling-optimizer` |
| Result[T] 类型 | LingFlow ↔ LingClaude | ~160 | 提取为共享包 |
| 异常层级 | 4 个项目 | ~1,000 | 提取为 `ling-exceptions` |
| 日志配置 | 7 个项目 | ~750 | 提取为 `ling-logging` |
| ConfigManager 模式 | LingFlow ↔ LingClaude | ~600 | 提取为 `ling-config` |

**注意**: 这是长期工程，不急。可以先把 `ling-protocol` 包做起来放 Result[T] 和异常。

---

## 五、优先级 P3 — 跨项目依赖梳理

### P3-1. sys.path.insert 依赖图

共 25+ 处硬编码路径，依赖关系：

```
LingMessage ← 全灵族依赖（消息、MCP注册）  ← 最核心
LingMinOpt  ← 6个项目依赖（优化引擎）
LingYi      ← 4个项目依赖（LLM调用封装）
LingFlow_plus ← 4个项目依赖（MCP注册表、项目管理）
```

### P3-2. 建议改进

1. **LingMessage 应该 pip install -e**: 所有项目通过 `pip install -e /home/ai/LingMessage` 引入，不用 sys.path
2. **LingMinOpt 同理**: 已有 setup.py，直接 `pip install -e`
3. **消除 sys.path.insert**: 全部改为正常的包依赖

---

## 六、优先级 P4 — .gitignore 补全

| 项目 | 缺失项 |
|------|--------|
| **ai-server** | **无 .gitignore** — 需创建 |
| **LingFlow_plus** | 缺 `.venv/`, `.env`, `*.db`, `node_modules/` |
| **lingresearch** | 缺 `.env`, `*.db` |
| **Ling-term-mcp** | 缺 `.venv/`, `__pycache__/`, `*.db`, `*.pyc` |
| **LingFlow** | 缺 `*.db`, `node_modules/` |
| **LingMessage** | 缺 `*.db` |
| **LingYang** | 缺 `*.db` |
| **lingtongask** | 缺 `*.db` |

---

## 七、优先级 P5 — 文档补全

### 无 README 的项目（需创建）
- **ai-server** — 完全无文档
- **ling-protocol** — 完全无文档

### 文档质量差的项目
- **LingWeb** — 有 README 但无安装/测试指南
- **LingYang** — 有 README 但无安装/测试指南
- **lingflow.top** — 缺 AGENTS.md
- **tryvoice-oss** — 缺 AGENTS.md

### 无 VERSION 文件的（14/21 个缺失）
建议至少在 README 中标注版本号。

---

## 八、项目活性分类

### 活跃（7天内有提交）— 正常维护
| 项目 | 最后提交 |
|------|----------|
| lingtongask | 4/17 |
| zhineng-knowledge-system | 4/17 |
| LingMessage | 4/16 |
| LingClaude | 4/16 |
| LingMinOpt | 4/16 |
| LingYi | 4/15 |
| LingFlow | 4/14 |
| LingYang | 4/14 |

### 温和（1-2周）— 需关注
| 项目 | 最后提交 |
|------|----------|
| lingresearch | 4/14 |
| LingFlow_plus | 4/14 |
| Ling-term-mcp | 4/14 |
| zhineng-bridge | 4/10 |

### 休眠（>2周）— 确认是否废弃
| 项目 | 最后提交 | 建议 |
|------|----------|------|
| lingflow.top | 4/8 | 小工具站，低频正常 |
| tryvoice-oss | 3/27 | 确认是否还在用 |
| lingflow-skills-index | — | 未安装在此主机 |
| lingflow-skills-example | — | 未安装在此主机 |

### 无 Git — 需要初始化或归档
| 项目 | 建议 |
|------|------|
| LingLaw | 有实质代码，建议 git init + push |
| LingWeb | 有实质代码，建议 git init + push |
| ai-server | 不明，需确认 |
| ling-protocol | 仅有骨架，建议 git init |
| ling-family-docs | 文档站，建议 git init + push |

---

## 九、LingFlow 专项 — 自家清洗清单

### 可安全删除的模块（确认后）

| 模块 | 原因 |
|------|------|
| `lingflow/monitoring/` | 0 个主代码引用 |
| `lingflow/communication/` | 0 个外部引用（测试仅引用自己） |
| `lingflow/infrastructure/` | 仅 cluster.py，0 外部引用 |
| `lingflow/intelligence/` | 仅 1 个外部引用 |
| `lingflow/ai_friendly.py` | 0 外部引用，347 行 |
| `lingflow/bootstrap.py` | 0 外部引用，204 行 |
| `lingflow/cli_helpers.py` | 0 外部引用，157 行 |

### 过度工程待简化

| 组件 | 现状 | 建议 |
|------|------|------|
| skills/ 33 个 | 全是文档+桩代码 | 保留 SKILL.md，删除空壳 implementation.py |
| agents.v2.json | 从未被读取 | 删除，统一用 agents.json |
| self_optimizer Phase 4-5 | 有实现但无调用者 | 标记为实验性，不删 |

### 双 venv 问题

LingFlow 有两个虚拟环境：
- `.venv/` — 5.4 GB（含 torch/numpy/pandas 等重量级包）
- `venv/` — 77 MB（轻量级）

**建议**: 确认哪个在用，删除另一个。

---

## 十、执行计划（建议顺序）

### 第一步：安全（今天）
- [ ] P0-1: 轮换 DashScope Key，改用环境变量
- [ ] P0-2: 删除/禁用泄露的 GitHub PAT
- [ ] P0-3: 禁用泄露的阿里云 AK
- [ ] P0-4: 确认 GLM Key 不在 Git 中

### 第二步：磁盘（本周）
- [ ] P1-1: 删除 ML 旧 checkpoints（5-15 GB）
- [ ] P1-2: 删除 Bilibili 归档视频（5-20 GB）
- [ ] P1-5: 删除 episodes 临时文件
- [ ] P1-6: 删除 .wine（如不需要）
- [ ] P1-8: 删除废弃的 zhineng_knowledge-system
- [ ] P1-9: 删除 .benchmarks
- [ ] P1-10: 删除 .bak 文件

### 第三步：代码卫生（本周）
- [ ] P4: 补全 .gitignore（特别是 ai-server 需新建）
- [ ] P2-3: 归档 lingtongask 根目录的一次性脚本
- [ ] LingFlow 双 venv 确认并删除多余的

### 第四步：深度清理（下周）
- [ ] P2-1: LingFlow 死代码标记/删除
- [ ] P3-2: 消除 sys.path.insert，改为 pip install -e
- [ ] P2-4: 考虑提取共享包

### 第五步：文档（下周）
- [ ] P5: 为 ai-server、ling-protocol 创建 README
- [ ] 无 Git 项目初始化仓库

---

## 附录 A：磁盘占用快照

| 目录 | 大小 | 说明 |
|------|------|------|
| zhineng-knowledge-system/ | 15 GB | 含 6.3G guoxue.db |
| LingFlow/ | 5.7 GB | 含双 venv (5.4G + 77M) |
| lingtongask/ | 4.9 GB | 音视频媒体为主 |
| .local/ | 5.9 GB | 系统包 |
| lingresearch/ | 2.5 GB | 含 ML models |
| crush.db 合计 | 0.9 GB | 15 个独立数据库 |

## 附录 B：跨项目依赖图

```
LingMessage ←── 全灵族（消息、MCP注册、讨论）
    ↑
LingYi ←── LingFlow, LingClaude, lingtongask, zhineng-ks（LLM封装）
LingMinOpt ←── LingFlow, LingClaude, lingresearch, lingtongask, zhineng-bridge, Ling-term-mcp
LingFlow_plus ←── LingClaude, lingresearch, LingLaw, lingflow.top（MCP注册表）

无外部依赖: LingFlow（独立工作流引擎）
```

## 附录 C：安全泄露清单

| 类型 | 数量 | 严重性 |
|------|------|--------|
| DashScope API Key | 10 处 | 🔴 CRITICAL |
| GitHub PAT | 5+ 处 | 🔴 CRITICAL |
| 阿里云 AK/SK | 3+ 处 | 🔴 CRITICAL |
| GLM API Key | 1 处 | 🟡 HIGH |
| iFlow Key | 3 处 | 🟡 HIGH |
| .gitignore 缺 *.db | 5 个项目 | 🟠 MEDIUM |
